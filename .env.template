# # Create a copy of this named .env and fill in the proper values

# # Database
# POSTGRES_DB=medusa-store
# POSTGRES_USER=postgres
# POSTGRES_PASSWORD=postgres

# # Medusa backend
# JWT_SECRET=supersecret
# COOKIE_SECRET=supersecret
# STORE_CORS=http://localhost:3000,http://localhost:8000
# ADMIN_CORS=http://localhost:9000,http://localhost:7000,http://localhost:3000
# AUTH_CORS=http://localhost:9000,http://localhost:3000
# NODE_ENV=development
# # Set to true/false to control seeding in start.sh (set to false by default unless you add a seed script)
# SEED_DB=true

# # Storefront
# # Do not set MEDUSA_BACKEND_URL here; it can leak the internal host to the browser/admin.
# # The storefront container will use MEDUSA_INTERNAL_BACKEND_URL injected via docker-compose.
# NEXT_PUBLIC_MEDUSA_PUBLISHABLE_KEY=pk_837b73ce30c90409438d93ea417e6a73062cc32d7b15a3ffcdd70e8af76782eb


# # Bootstrap script configuration (optional)
# # Override the storefront repository and branch/tag used by scripts/bootstrap.*
# STOREFRONT_REPO=https://github.com/medusajs/nextjs-starter-medusa
# STOREFRONT_REF=main
# # Optionally customize where apps live
# BACKEND_DIR=backend
# STOREFRONT_DIR=storefront

# # Backend initialization
# # Command template used by bootstrap to initialize the backend. {dir} is replaced with BACKEND_DIR.
# # Quote to ensure sourcing .env does not try to execute the value.
# BACKEND_INIT_CMD="npx create-medusa-app@latest {dir}"

# # Backend starter (optional; if set, bootstrap will clone this instead of running BACKEND_INIT_CMD)
# BACKEND_REPO=https://github.com/medusajs/medusa-starter-default
# BACKEND_REF=master

##########################################
# DATABASE (Supabase)
##########################################
DATABASE_URL=postgresql://postgres.sxwidguvxzlkbkkycttj:232399@aws-1-eu-west-1.pooler.supabase.com:6543/postgres
DATABASE_EXTRA={"ssl":{"rejectUnauthorized": false}}

##########################################
# MEDUSA BACKEND
##########################################
JWT_SECRET=supersecret
COOKIE_SECRET=supersecret

STORE_CORS=http://localhost:8000
ADMIN_CORS=http://localhost:9000
AUTH_CORS=http://localhost:8000,http://localhost:9000

NODE_ENV=development
SEED_DB=true

##########################################
# STOREFRONT
##########################################
NEXT_PUBLIC_MEDUSA_PUBLISHABLE_KEY=pk_ef84ab639ccb1a3bacd81973b35bc8086ac98788d1c0e9ddc46eb8f97db88411
NEXT_PUBLIC_MEDUSA_BACKEND_URL=http://localhost:9000
MEDUSA_BACKEND_URL=http://localhost:9000
MEDUSA_INTERNAL_BACKEND_URL=http://medusa:9000

##########################################
# BOOTSTRAP SCRIPT (optional)
##########################################
STOREFRONT_REPO=https://github.com/medusajs/nextjs-starter-medusa
STOREFRONT_REF=main
BACKEND_DIR=backend
STOREFRONT_DIR=storefront
BACKEND_INIT_CMD="npx create-medusa-app@latest {dir}"
BACKEND_REPO=https://github.com/medusajs/medusa-starter-default
BACKEND_REF=master
